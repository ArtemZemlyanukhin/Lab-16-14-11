# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. Среднее арифметическое элементов массива, расположенных
между первым и вторым отрицательными элементами
## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2.  Печать массива
3.  Поиск порядкового номера минимального значения элементов,
больших заданного значения А


## 📋 Задания к лаб. 16

1. Напишите программу, которая работает с динамическим массивом
2.  Перепишите программу так, чтобы массив хранил значения типа double, заполните его случайными числами от -1 до 1. 2.1. Удаление из массива каждого k-го элемента. 2.2. Вставка после каждого k-го элемента.
3. 
## 📁 Структура проекта



## 🔧 Реализованные функции

- ### 1. `double* full_elements(double* ptr_array, int size);`
- **Назначение**: Заполнение массива значениями
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `size` - размер массива
- **Возвращает**: указатель на заполненный массив
### 2. `double* calc_elements(double* ptr_array, int size);`
- **Назначение**: заполнение отрицательных значений массива нулями
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `size` - размер массива
- **Возвращает**: указатель на заполненный массив
- ### 3. `int put_elements(double* ptr_array, int size);`
- **Назначение**: вывод на экран элементов массива
- **Параметры**: 
  - `ptr_array` - указатель на массив
  - `size` - размер массива
- **Возвращает**: указатель на заполненный массив
- ### 4. `int delete_k(double* ptr_arr, int size, int k, int count);
- **Назначение**-удаление некоторого количества элементов массива
- ### 5. `double* insert(double* ptr_arr, int* size, int index, double num);`
- **Назначение**-
- ### 6. `double* insert_after_min_k(double* ptr_arr, int* size, int k, double num);`
- **Назначение**-вставка -999 после минимального числа массива
- ### 7. `int find_min_index(double* ptr_arr, int size);`
- **Назначение**-поиск индекса с наименьшим числом
- ### 8. `double task_14(int size);`
- **Назначение**-Поиск порядкового номера минимального значения элементов,
больших заданного значения А
- ### 9. `task_for_lab11(int size);`
- **Назначение**-Среднее арифметическое элементов массива, расположенных
между первым и вторым отрицательными элементами
- ### 10. `double* create_random_array(int* size);`
- **Назначение**-формирование массива d
- ### 11. `double* create_d_array(double* a, int size_a, double* b, int size_b, double* c, int size_c, int size_d);`
- ### 12. `void printf_all_arrays();`
- **Назначение**-вывод массивов d,a,b,c








